Add a toggle to turn the wakeword listening off
A camera mode where you can just scan the qrcode or enter the IP address to connect to a device in lan at port 6769
a manual input field in the app which works the same as wake word + command , only difference is you can silently provide a command. 
a manual wake button (Done)