Add a toggle to turn the wakeword listening off , so I can kill the background process whenever I needs and turn it back on again a way to save power. 

A camera mode where you can just scan the qrcode to connect to a device in lan at port 6769

a manual input field in the app which works the same as wake word + command , only difference is you can silently provide a command by writing and pressing enter. (Done)

a manual wake button (Done)

a wakeword training option for specipic user and ability to train any other word which works better for waking by being audibaly distinct and easier to detect.

a way for computer to make the phone speak back and alert me about notifications

A websocket connection instead of a tcp (optional){
    tcp for when both devices keep shuffling in and out of connection  , websocket when connection is stable.
}