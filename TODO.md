1. Add extension based script runner like the one used in Jarvis , it allows user to run 

2. Add a new toggle to the advance section called "preload commands" , enabling this makes the chat box appear and user can type and send commands without connecting to server , as soon as server is connected all the commands will be sent to the server in the order they came , this will allow user to prewrite commands and then connect to servers.    

3. The browser client needs a setting section. Android now has a settings section, animations, sounds, and a hide input toggle. ✅ (Android part done)

4. What happens when I send a command while replying to a non question message? does it include the previous message in the command? and is it sent to the same client?

